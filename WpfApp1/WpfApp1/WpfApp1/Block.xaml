<Window x:Class="WpfApp1.block"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gmail Home" Height="600" Width="800">
    <Window.Resources>
        <Style x:Key="GmailButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#f2f9fd"/>
            <Setter Property="Foreground" Value="#1e87f0"/>
            <Setter Property="BorderBrush" Value="#1e87f0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f9fbfd"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#d8e9f9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Background="#f2f2f2" Margin="10">
            <TextBlock Text="John Doe" VerticalAlignment="Center" Margin="10" Height="32" Width="206" Name="username"/>
            <Border Background="#f2f2f2" Width="2" Margin="10"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

            </StackPanel>
        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <StackPanel Grid.Column="0" Background="WhiteSmoke">
                <Button Content="Inbox" Style="{StaticResource GmailButtonStyle}" Click="Inbox"/>
                <Button Content="Send" Style="{StaticResource GmailButtonStyle}" Click="send"/>
                <Button Content="All Mail" Style="{StaticResource GmailButtonStyle}" Click="all_mail"/>
                <Button Content="Block" Style="{StaticResource GmailButtonStyle}" Click="Block"/>
                <Button Content="Log out" Style="{StaticResource GmailButtonStyle}" Click="logout"/>

            </StackPanel>

            <!-- Email List -->
            <TextBlock Grid.Column="1" Margin="10,410,286.333,28.667" Name="error" />

            <Button Content="submit" Click="submit" Margin="349,11,71.333,457.667" Grid.Column="1"/>

            <TextBlock Grid.Column="1" Text="username to block?" Height="30" Margin="4,11,428.333,457.667" />
            <TextBox Text="" Width="200" Height="30" Margin="120,11,223.333,457.667" Grid.Column="1" Name="target" />
            <Button Content="submit" Click="submit1" Margin="349,63,71.333,405.667" Grid.Column="1"/>

            <TextBlock Grid.Column="1" Text="username to unblock?" Height="30" Margin="4,63,427.333,405.667" />
            <TextBox Text="" Width="200" Height="30" Margin="120,63,223.333,405.667" Grid.Column="1" Name="untarget" />
            <TextBlock Grid.Column="1" Text="blocked users:" Height="30" Margin="4,98,426.333,370.667" />
            <TextBlock Grid.Column="1" Text="" Margin="4,128,286.333,114.667" Name="list" />


        </Grid>
    </Grid>
</Window>